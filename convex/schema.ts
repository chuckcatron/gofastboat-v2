import { defineSchema } from "convex/server";

// Full inventory schema (boats, boatImages, products, pageContent) is defined
// in AB#1238. This placeholder establishes the Convex schema entry point so the
// backend dir is wired during scaffolding.
export default defineSchema({});
